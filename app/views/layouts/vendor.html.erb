<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : t(:app_name) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#     <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css"> %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <meta name="turbo-refresh-scroll" content="preserve">
    <%= yield(:head) %>
  </head>
  <body>
    <main class="relative">
      <aside class="hidden md:block fixed font-medium bg-white border-r border-gray-300 h-screen w-[264px] z-50 overflow-y-auto overflow-x-hidden pb-9 px-2">
        <%= render "shared/vendor_sidebar" %>
      </aside>
      <header>
        <%= render "shared/flash" %>
        <%= render "shared/vendor_navbar" %>
      </header>
      <section class="flex flex-col min-h-screen pt-16 md:pl-[264px] w-full bg-gray-100">
        <%= content_for?(:body) ? yield(:body) : yield %>
      </section>
    </main>
  </body>
</html>
