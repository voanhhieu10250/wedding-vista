<%= turbo_frame_tag dom_id(@common_question) do %>
  <%= render "form", path: vendor_service_question_path(params[:service_id], @common_question),
                     method: :patch,
                     question: @common_question,
                     turbo_frame_target: nil %>

  <br>

  <div>
    <%= link_to "Show this common question", vendor_service_question_path(params[:service_id], @common_question) %> |
  </div>
<% end %>
