<div id="<%= dom_id gallery %>">
  <p>
    <strong>Service:</strong>
    <%= gallery.service_id %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= gallery.name %>
  </p>

  <p>
    <strong>Items:</strong>
    <% gallery.items.each do |item| %>
      <div>
        <%= image_tag item.variant(:thumb) %>
        <%= link_to item.filename, item %>
        </div>
    <% end %>
  </p>

</div>
