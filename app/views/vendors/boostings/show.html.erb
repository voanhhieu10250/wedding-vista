<p style="color: green"><%= notice %></p>

<%= render partial: "priority_boosting", locals:{ priority_boosting: @priority_boosting } %>

<div>
  <%= link_to "Edit this priority boosting", edit_vendor_service_boosting_path(@priority_boosting.service, @priority_boosting) %> |
  <%= link_to "Back to priority boostings", vendor_service_boostings_path %>

  <%= button_to "Destroy this priority boosting", vendor_service_boosting_path(@priority_boosting.service, @priority_boosting), method: :delete %>
</div>
