<p style="color: green"><%= notice %></p>

<h1>Ideas</h1>

<div id="ideas">
  <% @ideas.each do |idea| %>
    <%= render idea %>
    <p>
      <%= link_to "Show this idea", vendor_idea_path(idea) %>
    </p>
  <% end %>
</div>

<%= link_to "New idea", new_vendor_idea_path %>
