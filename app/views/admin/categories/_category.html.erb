<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>

  <p>
    <strong>Image:</strong>
    <%= image_tag category.image.variant(:header_bg) if category.image.attached? %>
  </p>

</div>
