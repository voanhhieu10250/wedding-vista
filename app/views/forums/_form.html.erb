<%= form_with(model: forum) do |form| %>
  <% if forum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(forum.errors.count, "error") %> prohibited this forum from being saved:</h2>

      <ul>
        <% forum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :users_count, style: "display: block" %>
    <%= form.number_field :users_count %>
  </div>

  <div>
    <%= form.label :discussions_count, style: "display: block" %>
    <%= form.number_field :discussions_count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
